#pragma once

constexpr unsigned int side = 6;
constexpr unsigned int half_side = side / 2;
constexpr unsigned int board_size = side * side;
constexpr unsigned int board_half_size = board_size / 2;
